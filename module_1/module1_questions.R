### Q1  -> Afficher les 30 premiers éléments du tableau df
### Q2  -> Afficher le nombre de points effectués par chaque opérateur
### Q3  -> Afficher la somme de tous les points validés
### Q4  -> Afficher les noms des codes possibles dans la carte
### Q5  -> Afficher le nombre de points issus de la carte avec le code "route" (map_class)
### Q6  -> Afficher le nombre de points avec le code "Fm" (ref_class) trouvés par l'opérateur "manual"
### Q7  -> Créer une copie de df appelée my_df
### Q8  -> Dans my_df, remplacer le nom de l'opérateur "manual" par "CNIAF_visuel"
### Q9a -> Dans my_df, remplacer les codes (ref_class et map_class) "Fp", "Fm", "Fs" par "Foret"
### Q9b -> Dans my_df, remplacer les codes (ref_class et map_class) "Pp", "Pm", "Ps" et "route" par "Perte"
### Q9c -> remplacer les codes (ref_class et map_class) "NF", "Eau" par "Non-Foret"
### Q10 -> Faire la meme chose avec les codes "ref_class" 
### Q11 -> Afficher la matrice de confusion des classes aggrégées
